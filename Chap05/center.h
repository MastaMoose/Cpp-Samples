#ifndef GUARD_center_h
#define GAURD_grade_h

#include <vector>
#include <string>

std::string::size_type width(const std::vector<std::string>& v);
std::vector<std::string> center(const std::vector<std::string>& v);

#endif